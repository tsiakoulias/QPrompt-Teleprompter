# [W10-PLF-01] BSD detection broken — FreeBSD enters wrong code paths

- **Status:** OPEN
- **Severity:** Critical
- **Category:** Platform/Build
- **Location:** `CMakeLists.txt:250,487`
- **Consensus:** 6/6 agents LEGIT · AGREE

## Original report claim

- **File:** CMakeLists.txt:250,487
- **Severity:** Critical
- **Category:** Platform/Build
- **Analysis:** CMake has no standard `BSD` variable. `${BSD}` never set → `NOT BSD` always true → FreeBSD routes through wrong FetchContent/find_package and wrong CPack generator.
- **Impact:** FreeBSD build completely misconfigured. Wrong dependency resolution and packaging.

## Agent assessments

| Agent | Verdict | Conf | Rationale |
|---|---|---|---|
| opus | ✅ LEGIT | 70 | BSD is not a builtin CMake var and never set -> if(NOT BSD) guard always true (CMakeLists.txt:250) |
| gpt | ✅ LEGIT | 84 | BSD detection broken - FreeBSD enters wrong code paths (CMakeLists.txt:250) |
| deepseek | ✅ LEGIT | 95 | ${BSD} never set in CMake; NOT BSD always true; FreeBSD enters wrong FetchContent/CPack paths (CMakeLists.txt:250 487) |
| glm | ✅ LEGIT | 80 | CMakeLists.txt:250 487 BSD detection via if(BSD) may not be set correctly for FreeBSD; enters wrong code paths |
| kimi | ✅ LEGIT | 85 | CMakeLists.txt:250 uses NOT BSD and line 487 uses BSD; CMake does not define a BSD variable, so FreeBSD routing is wrong. |
| opus-ultra | ✅ LEGIT | 70 | BSD is not a builtin CMake var and never set -> if(NOT BSD) guard always true (CMakeLists.txt:250) |

## Patch  _(fill when fixing)_

- **Root cause:**
- **Fix:**
- **Files changed:**
- **Verification:**
- **Commit / PR:**

