# [065-W10-CNV2-02] No markdown export — round-trip silently destroys all formatting

- **Status:** FIXED  (saveAs writes toMarkdown() for .md/.markdown; commit 9053883 on fix/save-markdown-formatting)
- **Severity:** High
- **Category:** Logic
- **Location:** `src/documenthandler.cpp:1138-1177`
- **Consensus:** 6/6 agents LEGIT · AGREE

## Original report claim

- **File:** src/documenthandler.cpp:1138-1177
- **Severity:** High
- **Category:** Logic
- **Analysis:** Markdown imported via Qt::MarkdownText but saveAs only branches HTML/plain. .md extension → toPlainText() stripping all formatting. toMarkdown() never called.
- **Impact:** Opening .md, editing with formatting, saving → all bold/italic/images/hyperlinks destroyed.

## Agent assessments

| Agent | Verdict | Conf | Rationale |
|---|---|---|---|
| opus | ✅ LEGIT | 55 | no markdown export; .md saved as plaintext loses formatting (documenthandler.cpp:1138) |
| gpt | ✅ LEGIT | 84 | No markdown export - round-trip silently destroys all formatting (src/documenthandler.cpp:1138) |
| deepseek | ✅ LEGIT | 92 | saveAs only branches HTML/plain no MarkdownText export; .md->toPlainText destroys all formatting (documenthandler.cpp:1154 1164) |
| glm | ✅ LEGIT | 80 | documenthandler.cpp:1138-1177 save() only supports HTML and plain text; no markdown export round-trip |
| kimi | ✅ LEGIT | 90 | documenthandler.cpp:1138-1177 saveAs writes toHtml() or toPlainText(); it never calls toMarkdown(), so .md formatting is lost. |
| opus-ultra | ✅ LEGIT | 55 | no markdown export; .md saved as plaintext loses formatting (documenthandler.cpp:1138) |

## Patch  _(fill when fixing)_

- **Root cause:** Markdown is imported as **rich text** — `updateContents()` parses
  `text/markdown` via `QTextCursor::insertMarkdown()` (documenthandler.cpp:959-960, 1112-1113),
  so headings/bold/italic/links/images become real document formatting. But `saveAs()`
  (documenthandler.cpp:1138-1177) only distinguished HTML from everything else:
  `isHtml` is true for `.html/.htm/.xhtml`, and the writer was
  `file.write((isHtml ? doc->toHtml() : doc->toPlainText())...)`. A `.md` file took the
  `toPlainText()` branch, which flattens all formatting. `toMarkdown()` was never called.
- **Fix:** Add an `isMarkdown` flag (`.md` / `.markdown`, case-insensitive) and a third
  output branch: `isHtml ? toHtml() : isMarkdown ? toMarkdown() : toPlainText()`. The Android
  path keeps `isHtml = true` (and `isMarkdown = false`). `toMarkdown()`'s default
  `MarkdownDialectGitHub` matches the dialect `insertMarkdown()` uses on import, so the
  round trip is symmetric. The file-open mode is unchanged (Markdown writes as text, like
  plain text did).
- **Files changed:** `src/documenthandler.cpp` (saveAs: +3 lines, 1 line changed).
- **Verification:** Static. Confirmed import uses `insertMarkdown` (GitHub dialect) and that
  `QTextDocument::toMarkdown(MarkdownFeatures = MarkdownDialectGitHub)` exists in Qt 6
  (`QtGui/qtextdocument.h:126`) — both default to the same dialect. Round trip: open `.md`
  (parsed to rich text) → edit → save `.md` now serialises with `toMarkdown()` instead of
  `toPlainText()`, preserving formatting. Full Qt build not run on this Windows host (no
  configured build tree); change is a localised, self-contained use of a stable QtGui API.
- **Commit / PR:** `9053883` on branch `fix/save-markdown-formatting` (cut from `main`).

