# [029-EDGE-03] Empty container `last()` dereference in `extendLastMarker`

- **Status:** OPEN
- **Severity:** High
- **Category:** 
- **Location:** `src/markersmodel.cpp:111-113`
- **Consensus:** 6/6 agents LEGIT · AGREE

## Original report claim

- **File:** src/markersmodel.cpp:111-113
- **Severity:** High
- **Code:**
  ```cpp
  void MarkersModel::extendLastMarker(QString text) {
      const auto last = m_data.last();   // UB if empty
      if (last.text.length()==1)
          m_data.last().text += text;
  }
  ```
- **Analysis:** `QList::last()` on empty list is UB. Public method, no `isEmpty()` guard. Only internal caller (parse()) ensures a marker was just appended, but external callers could trigger the bug.
- **Impact:** Application crash if `extendLastMarker` is called with empty markers list.

## Agent assessments

| Agent | Verdict | Conf | Rationale |
|---|---|---|---|
| opus-4.8-extra | ✅ LEGIT | 90 | m_data.last() no guard (markersmodel.cpp:111) |
| gpt-5.5-xhigh | ✅ LEGIT | 84 | Empty container last() dereference in extendLastMarker (src/markersmodel.cpp:111) |
| deepseek-v4-pro-max | ✅ LEGIT | 85 | m_data.last() at markersmodel.cpp:111 on empty list is UB; public method no isEmpty() guard — crash if called with empty markers |
| glm-5.2-xhigh | ✅ LEGIT | 90 | markersmodel.cpp:111-113 m_data.last() called without checking if m_data is empty; crashes on empty model |
| kimi-k2.7-code | ✅ LEGIT | 90 | markersmodel.cpp:111-113 extendLastMarker calls m_data.last() without checking m_data.isEmpty(). |
| opus-4.8-ultra | ✅ LEGIT | 90 | m_data.last() no guard (markersmodel.cpp:111) |

## Patch  _(fill when fixing)_

- **Root cause:**
- **Fix:**
- **Files changed:**
- **Verification:**
- **Commit / PR:**

