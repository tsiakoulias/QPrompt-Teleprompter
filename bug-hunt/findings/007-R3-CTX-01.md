# [007-R3-CTX-01] AbstractUnits missing QML_ELEMENT — all duration constants resolve to undefined

- **Status:** FIXED  (aa2b6ab on fix/register-units-qml-type — QML_NAMED_ELEMENT(Units))
- **Severity:** Critical
- **Category:** QML/UI
- **Location:** `src/abstractunits.hpp:30`
- **Consensus:** 6/6 agents LEGIT · AGREE

## Original report claim

- **File:** src/abstractunits.hpp:30
- **Severity:** Critical
- **Category:** QML/UI
- **Analysis:** Class declares `QML_UNCREATABLE` but omits `QML_ELEMENT`. Without QML_ELEMENT, Qt never registers the type. The old Qt 5 registration in main.cpp:219 is commented out. 36 QML references to `Units.ShortDuration`, `Units.LongDuration`, `Units.VeryLongDuration`, `Units.HumanMoment` across 7 files all resolve to `undefined`.
- **Impact:** Every animation (fades, slides, pointer transitions) using these constants gets zero/undefined duration. App-wide visual experience broken.

---

## Agent assessments

| Agent | Verdict | Conf | Rationale |
|---|---|---|---|
| opus | ✅ LEGIT | 90 | AbstractUnits QML_UNCREATABLE no QML_ELEMENT; reg commented (abstractunits.hpp:30) |
| gpt | ✅ LEGIT | 92 | AbstractUnits missing QML_ELEMENT - all duration constants resolve to undefined (src/abstractunits.hpp:30) |
| deepseek | ✅ LEGIT | 95 | abstractunits.hpp:30 has QML_UNCREATABLE but missing QML_ELEMENT — Qt6 qmltyperegistrar never registers type; old Qt5 registration main.cpp:219 commented out |
| glm | ✅ LEGIT | 90 | abstractunits.hpp:30 AbstractUnits has QML_UNCREATABLE but not QML_ELEMENT; enum values resolve via QML_ELEMENT on the class which IS present. Actually QML_ELEMENT is missing. |
| kimi | ✅ LEGIT | 85 | abstractunits.hpp:30 has QML_UNCREATABLE but no QML_ELEMENT; enum durations have no registered QML type so bare Units references resolve undefined. |
| opus-ultra | ✅ LEGIT | 90 | AbstractUnits QML_UNCREATABLE no QML_ELEMENT; reg commented (abstractunits.hpp:30) |

## Patch  _(fill when fixing)_

- **Root cause:** `AbstractUnits` (src/abstractunits.hpp) declares `QML_UNCREATABLE` but
  no `QML_ELEMENT`/`QML_NAMED_ELEMENT`. Per Qt, `QML_UNCREATABLE` only marks an *already
  registered* element as uncreatable — on its own it registers nothing. The old
  `qmlRegisterUncreatableType(... "Units" ...)` in main.cpp:219 is commented out. The class is
  in the `com.cuperino.qprompt` QML module's SOURCES (src/CMakeLists.txt:79 → qt_add_qml_module
  URI com.cuperino.qprompt), but with no element macro `qmltyperegistrar` skipped it. QML
  refers to the type as `Units` (e.g. `duration: Units.ShortDuration`), so those references
  resolved to `undefined` and the animations/timers fell back to default timings.
- **Fix:** Add `QML_NAMED_ELEMENT(Units)` to `AbstractUnits` — one line. Registers it under the
  name the QML already uses, in the module those files import. (Did not touch the commented
  main.cpp line — minimal diff; it's now obsolete but harmless.)
- **Files changed:** `src/abstractunits.hpp` (+1 line).
- **Verification:** Incremental Craft build, EXIT 0, clean. The generated
  `build-craft/src/qprompt_qmltyperegistrations.cpp` now emits
  `qmlRegisterTypesAndRevisions<AbstractUnits>("com.cuperino.qprompt", 1);` +
  `qmlRegisterEnum<AbstractUnits::Durations>(...)` (absent before the fix), and qmlcachegen
  recompiled the Units consumers (Flip/Find/Prompter) cleanly. So bare `Units.<Duration>` now
  resolves.
- **Separate issue (NOT fixed here):** several QML sites use bare `Units` with *Kirigami*
  property names — `Units.smallSpacing`, `Units.longDuration`, `Units.mediumSpacing` — and a few
  capitalized typos `Units.SmallSpacing` / `Kirigami.Units.SmallSpacing`. None of those are
  members of `AbstractUnits::Durations`; they should be `Kirigami.Units.smallSpacing` etc. This
  fix does not address (or regress) them — they were undefined before and remain so. Worth its
  own ticket. Many live in src/kirigami_ui/EditorToolbar.qml + PrompterPage.qml.
- **Commit / PR:** `aa2b6ab` on `fix/register-units-qml-type`.

