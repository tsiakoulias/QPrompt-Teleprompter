# [533-EDGE-07] Q_UNREACHABLE in Q_INVOKABLE method — UB if called from QML

- **Status:** OPEN
- **Severity:** High
- **Category:** 
- **Location:** `src/shakedetector.cpp:49-51`
- **Consensus:** 2/6 agents LEGIT · split

## Original report claim

- **File:** src/shakedetector.cpp:49-51
- **Severity:** High
- **Code:**
  ```cpp
  void ShakeDetector::showUndoRedoDialog(bool, bool) {
      Q_UNREACHABLE();   // but method is Q_INVOKABLE!
  }
  ```
- **Analysis:** `showUndoRedoDialog` is declared `Q_INVOKABLE` (callable from QML). Implementation uses `Q_UNREACHABLE()` which compiles to `__builtin_unreachable()` in release — invoking UB if the path is ever taken. Should be a no-op.
- **Impact:** Undefined behavior (crash, miscompile, or exploit) if QML calls this method.

## Agent assessments

| Agent | Verdict | Conf | Rationale |
|---|---|---|---|
| opus-4.8-extra | ⚠️ PARTIAL | 60 | Q_UNREACHABLE in Q_INVOKABLE but only caller fires on iOS (shakedetector.cpp:51) |
| gpt-5.5-xhigh | ⚠️ PARTIAL | 58 | observed Q_UNREACHABLE in Q_INVOKABLE method - UB if called from QML (src/shakedetector.cpp:49) |
| deepseek-v4-pro-max | ✅ LEGIT | 85 | Q_UNREACHABLE() at shakedetector.cpp:49-51 in Q_INVOKABLE showUndoRedoDialog; compiles to __builtin_unreachable() in release — UB if QML calls this |
| glm-5.2-xhigh | ⚠️ PARTIAL | 60 | shakedetector.cpp:49-51 Q_UNREACHABLE in showUndoRedoDialog; reachable from QML but non-iOS .cpp build has empty body not Q_UNREACHABLE |
| kimi-k2.7-code | ✅ LEGIT | 90 | shakedetector.cpp:49-51 Q_UNREACHABLE in Q_INVOKABLE showUndoRedoDialog; reachable from QML -> UB. |
| opus-4.8-ultra | ⚠️ PARTIAL | 50 | max: real but non-behavioral (style/arch/non-issue) — Q_UNREACHABLE in Q_INVOKABLE but only caller fires on iOS (shakedetector.cpp:51) |

## Patch  _(fill when fixing)_

- **Root cause:**
- **Fix:**
- **Files changed:**
- **Verification:**
- **Commit / PR:**

