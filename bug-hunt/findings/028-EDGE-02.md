# [028-EDGE-02] Empty container `first()` dereference — crash on hotkey with no windows

- **Status:** OPEN
- **Severity:** High
- **Category:** 
- **Location:** `src/main.cpp:272`
- **Consensus:** 6/6 agents LEGIT · AGREE

## Original report claim

- **File:** src/main.cpp:272
- **Severity:** High
- **Code:**
  ```cpp
  QWindowList windows = app.allWindows();
  QWindow *topWindow = windows.first();  // UB if empty
  const bool initiallyOpaque = topWindow->opacity() == 1.0;
  ```
- **Analysis:** `QList::first()` on empty list is UB. If Meta+Alt+F10 is pressed before any window exists or after all closed, the list is empty. No `isEmpty()` guard.
- **Impact:** Application crashes on transparency toggle hotkey in windowless state.

## Agent assessments

| Agent | Verdict | Conf | Rationale |
|---|---|---|---|
| opus-4.8-extra | ✅ LEGIT | 95 | windows.first() UB if empty (main.cpp:272) |
| gpt-5.5-xhigh | ✅ LEGIT | 84 | Empty container first() dereference - crash on hotkey with no windows (src/main.cpp:272) |
| deepseek-v4-pro-max | ✅ LEGIT | 90 | windows.first() at main.cpp:272 on empty list is UB; no isEmpty() guard — crash on Meta+Alt+F10 with no windows |
| glm-5.2-xhigh | ✅ LEGIT | 85 | main.cpp:272 windows.first() called without checking if windows list is empty; crashes if no windows |
| kimi-k2.7-code | ✅ LEGIT | 90 | main.cpp:272 QWindowList::first() called without isEmpty() guard; UB when no windows exist. |
| opus-4.8-ultra | ✅ LEGIT | 95 | windows.first() UB if empty (main.cpp:272) |

## Patch  _(fill when fixing)_

- **Root cause:**
- **Fix:**
- **Files changed:**
- **Verification:**
- **Commit / PR:**

